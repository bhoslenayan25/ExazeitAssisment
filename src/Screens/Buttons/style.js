import { StyleSheet } from 'react-native';
import { colorCode } from '../../Utilities/AppConstants';

export default StyleSheet.create({
    container: { flex: 1, padding: 30, marginTop: 28, backgroundColor: colorCode.BLACK },
    buttonStyle: { marginTop: 20, paddingVertical: 10, height: 50, width: '100%', borderRadius: 5, alignContent: 'center', justifyContent: 'center' }
});
